<?xml version="1.0" encoding="utf-8"?>
<android.support.design.widget.CoordinatorLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fitsSystemWindows="true"
    android:background="@drawable/background"
    tools:context="au.edu.cdu.solarnt.HelpActivity">

    <android.support.design.widget.AppBarLayout
        android:id="@+id/app_bar"
        android:layout_width="match_parent"
        android:layout_height="@dimen/app_bar_height"
        android:background="@android:color/transparent"
        android:fitsSystemWindows="true"
        android:theme="@style/AppTheme.AppBarOverlay">

        <android.support.design.widget.CollapsingToolbarLayout
            android:id="@+id/toolbar_layout"
            android:layout_width="match_parent"
            android:layout_height="125dp"
            android:fitsSystemWindows="true"
            app:contentScrim="@color/black_overlay"
            app:layout_scrollFlags="scroll|exitUntilCollapsed"
            app:statusBarScrim="@color/black_overlay">

            <android.support.v7.widget.Toolbar
                android:id="@+id/toolbar"
                android:layout_width="match_parent"
                android:layout_height="?attr/actionBarSize"
                app:layout_collapseMode="pin"
                app:popupTheme="@style/AppTheme.PopupOverlay" />

        </android.support.design.widget.CollapsingToolbarLayout>
    </android.support.design.widget.AppBarLayout>

    <include
        android:id="@+id/includeHome"
        layout="@layout/help_home"
        android:visibility="invisible" />

    <include
        android:id="@+id/includeDashboard"
        layout="@layout/help_dashboard"
        android:visibility="invisible" />

    <include
        android:id="@+id/includeDusting"
        layout="@layout/help_dusting"
        android:visibility="invisible" />

    <include
        android:id="@+id/includeCompare"
        layout="@layout/help_compare"
        android:visibility="invisible" />

    <include
        android:id="@+id/includeOutputs"
        layout="@layout/help_outputs"
        android:visibility="invisible" />

    <include
        android:id="@+id/includeProjections"
        layout="@layout/help_projections"
        android:visibility="invisible" />

    <include
        android:id="@+id/includeProviders"
        layout="@layout/help_providers"
        android:visibility="invisible" />

    <include
        android:id="@+id/includeWeather"
        layout="@layout/help_weather"
        android:visibility="invisible" />

    <android.support.design.widget.FloatingActionButton
        android:id="@+id/fab"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_margin="@dimen/fab_margin"
        app:layout_anchor="@id/app_bar"
        app:layout_anchorGravity="bottom|end"
        app:srcCompat="@android:drawable/ic_dialog_email" />

</android.support.design.widget.CoordinatorLayout>
